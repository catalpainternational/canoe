// js/SearchAndFiltering

/** Note that the module name here MUST match how it's used in the .ts files */
declare module "js/SearchAndFiltering" {
    export function getTagsFromPages(pages: any[]): string[];
}
